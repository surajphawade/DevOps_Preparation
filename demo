trim(
  first(
    skip(
      outputs('Compose_-_Columns'),
      int(
        xpath(
          xml(
            concat(
              '<r><c>',
              join(
                outputs('Compose_-_Headers'),
                '</c><c>'
              ),
              '</c></r>'
            )
          ),
          'count(/r/c[normalize-space(.)="Run ID"]/preceding-sibling::c)'
        )
      )
    )
  )
)